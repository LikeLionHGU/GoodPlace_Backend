# 05. 결정 대기 및 다음 할 일 인수인계

## 이번 대화에서 확정된 변경 (반드시 기억)

**★ 2026.07.14 대전환 (최우선):** ① 투표 = 동네+업종(공실 아님), 위치 = 행동 범위·200m 격자 ② 수요 = 거리 감쇠(≤500m 1.0 / 500~800m 선형 / 초과 0, w1 폐기) ③ 창업자 리포트 = 업종 클릭 → 추천 공실 1·2·3위 ④ 공실 등록 중개사 필드(층·용도·간이설비·임대조건) ⑤ **AI 해설 = GPT API(OpenAI, 팀 보유 키)**(환각 억제). 명세 = 08번, 로드맵 = CLAUDE_통합본 v2 P1~P4.

1. **서비스명: 명당** (확정. 구 임시명 '동네주문'). 기존 산출물(답변집·API명세서·백엔드 코드 주석·기획서)의 '동네주문' 표기는 다음 대화에서 일괄 교체 대상.
2. **개발·1차 검증 기준지: 포항 양덕동.** 단 **서비스 구조는 경북 전체 확장 가능하게 설계** — 지역을 행정동/시군구 코드로 파라미터화해, 양덕동은 첫 적용지일 뿐 코드 변경 없이 경북 다른 지역도 동작하도록. (기획서의 '중앙상가' 통계는 문제 규모 근거로 유지, 개발 검증지와 구분)
3. **공실·상권 데이터: 소상공인 공공 API**(B553077)로 수집. 단 API 한계로 인구·유동인구는 별도 소스 필요(04번 검증 결과 참조).
3. **결제: 모의(플로우 시연만).** 사업자등록 불가 → 실결제·실PG 없음. 결제 화면과 상태 흐름만 보여줌. 전자금융거래법·정산 리스크 없음.
4. **환불 = 캐시 적립(모의).** 그냥 환불이 아니라 캐시로 돌려주고, 그 캐시로 ① 다른 투표에 사용 ② 우리 서비스로 창업한 매장의 할인쿠폰 구매·사용. 이것도 모의라 상태·화면 흐름만.

## 팀이 정해야 코드로 옮길 수 있는 것

### 데이터·환경 (04번)
- [ ] 공공데이터포털 serviceKey 발급
- [ ] 양덕동 행정동 코드 확정
- [ ] `storeListInAdmi` 첫 호출 검증 (파라미터명·필드·폐업 소급범위·호출한도)
- [ ] 업종 6종 ↔ 247 코드 매핑

### 배치 알고리즘 (03번 v2)
- [x] ~~헝가리안 vs 공실 수 제한~~ → **헝가리안 확정**(scipy, 폴백 자동). 코드 구현·검증 완료

### 적합도 산식 (03번 v2)
- [x] ~~가중치 초기값~~ → **w1=0.4, w2=1.0 확정**(근거 포함). 경쟁은 동네 평균 대비 비율식
- [x] ~~산식 이원화~~ → **표시 % = 포화 곡선 통일** 확정·구현(요소 재조합 금지)
- [x] ~~이전 매장 정보~~ → **리포트 참고 정보로만**(점수 미반영) 구현
- [ ] **K 전역 고정 vs 캠페인별** + BASELINE_TARGET_VOTES(잠정 30) 실데이터 캘리브레이션
- [ ] **층수 계수의 자리**: 참고 정보 유지 vs 산식에 켜기
- [ ] 인구 데이터 소스 (확장 요소용 — 소상공인 API엔 없음, 04번)

### 캐시·결제 흐름 (config.CASH_POLICY — 코드에 미확정 표기됨)
- [x] ~~캐시 모델링~~ → **cash_ledger 원장**(적립/사용, reason=refund/vote/coupon) 뼈대 구현
- [ ] 캐시 교환 비율(1캐시=1원?), 유효기간 유무
- [ ] 캐시로 재투표 시 현금 투표와 동일 취급 여부
- [ ] 할인쿠폰 구매 흐름 (창업 성사 매장 목록 → 데이터 모델 확장)
- [ ] 모의 결제 화면 단계(장바구니→결제수단→완료) 어디까지 보여줄지

### 🚫 보류 (근거 게이트 미통과 — 재개 조건 충족 전 구현 금지)
- **지역 특성 노트** — 재개 조건: 용도지역·인구 데이터 소스(SGIS 등) 확보
- **투표자 거주 분포** — 재개 조건: votes 스키마에 위치 수집 설계 추가(개인정보 처리 검토 포함)
- **월 고정비 참고** — 재개 조건: 공단 업종 평균 운영비 실출처 확보
- (원칙) 새 기능은 07번 '근거 게이트' 3문항 통과 후에만 추가

### 기타
- [ ] 시드 업종 6종·면적·창업비용·인허가 값 발표 승인 (seed_dummy.py — 현재 임시)
- [ ] 캠페인 기한·쿠폰 액면가 (현재 2026-09-30·3,000원)
- [ ] 시연에서 시드/실데이터 구분 고지 문구
- [ ] region_code 4711158000(시드값)이 실제 양덕동 행정동 코드인지 확인

## 다음에 바로 이어서 할 일 (전환 로드맵 — CLAUDE_통합본 v2 §11)

1. **P1 스키마 v3:** votes 개편(vacancy_id 제거·voter_grid 추가) + vacancies 중개사 필드 + 마이그레이션 (voter_name 이슈 흡수)
2. **P2 수요 엔진:** 거리 감쇠 demand 교체, grid 집계 어댑터 (검증: 감쇠 단조성·경계값·손계산)
3. **P3 리포트 재구조화:** 업종→공실 순위, 3층 판정, 탭=공실 순위 (demo·README 스테일 함께 수정)
4. **P4 AI 해설:** GPT API 연동(.env, 보유 키), 환각 억제 프롬프트, 숫자 화이트리스트 검사
5. 6단계(캐시 결정 선행) → 7단계(serviceKey·행정동 코드 선행)

## 새 대화 시작 시 붙여넣을 요약 (복붙용)

> 팀 사다리타기의 공실 해결 플랫폼 **'명당'** — 2026.07.14 **동네 투표 모델로 대전환**했습니다. 주민은 동네+업종에 투표(위치=행동 범위·200m 격자), 수요는 거리 감쇠(500/800m), 창업자는 업종 클릭→추천 공실 1·2·3위 리포트(AI 해설=Claude API, 환각 억제)를 봅니다. **현재 코드(mini-main)는 구모델이라 전환 필요** — 08번 명세와 CLAUDE_통합본 v2의 P1(스키마)~P4(AI 해설) 순서로, 단계별 검증하며 진행합니다. 구모델 자산(면적·경쟁계수·포화 곡선·헝가리안, 테스트 97개)은 재사용합니다.

## 지침 리마인더 (작업 톤)

객관적 분석 먼저, 프로젝트 적용은 그다음. 과장 금지, 부재 증명은 "조사한 범위에서는"으로 한정. 미확정을 확정처럼 쓰지 않기. 반대 데이터 숨기지 않기. 투자·펀딩 대신 선결제·쿠폰 선구매. 출처 없는 수치 금지.
