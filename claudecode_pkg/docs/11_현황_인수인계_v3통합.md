# 11. 현황·인수인계 — v3 통합 (단일 최신본)

**2026.07.15 작성.** 이 문서가 **현재 상태의 단일 진실**이다. 다른 인수인계 문서(00~09)는
v1/v2 시점 스냅샷이라 아래와 충돌하면 **이 문서와 코드가 우선**. 새 대화는 이 문서부터 읽는다.

> 한 줄: **담당 A의 v3 데이터 계층(Sadari_Backend)을 우리 저장소로 채택하고, 담당 B의 엔진을
> v3로 통합 중.** A 데이터계층 + B 엔진(v3)까지 완료, 리포트·AI해설·캠페인은 v3 포팅 남음.

---

## 1. 지금 아키텍처

```
[프런트(별도 담당)] → [FastAPI main.py]
   A 데이터계층                         B 계산자산(순수 함수)
   database.py  (v3 스키마·집계뷰)       engine.py   ✅ v3 완료
   seed.py      (업종6·공실3·투표4)       report.py   🔜 v3 포팅
   routes_vote  (다중선택·격자·냥)         campaign.py 🔜 v3 포팅
   routes_cash / routes_region           ai_explain.py 🔜 v3 facts
   ingest_api   (소상공인 API)            adapters.py  🔜 v3 어댑터
```
- **id = INTEGER**(A 방식). **DB = meongdang.db**. 서버 기동 시 `.env` 로드(main.py).
- **B 집계 계약**: `database.get_vote_grid_counts()` → `{(region_code, industry_id, voter_grid): 표수}`.
  엔진이 voter_grid(`"lat,lng"`)를 파싱해 공실까지 거리감쇠 수요를 계산한다.

## 2. 완료된 것

**A 데이터계층 (I0 · 채택·검증·push):**
- votes v3(region_code+industry_id+voter_grid, 공실 아님), 다중선택 `/votes/batch`+냥 일괄차감,
  `snap_to_grid`(200m), 동네수요 `/regions/{code}/demand`, 냥 캐시(credit/use/balance, 10원=1냥),
  placements 테이블, 소상공인 ingest(`/admin/ingest`).
- 우리 스택 통일: ingest `httpx`·env `SBIZ_SERVICE_KEY`, main `.env` 로딩.

**B 엔진 (I1 · 완료·검증, 커밋 대기):**
- `score = demand × area_fit × competition_factor × floor_fit`
- demand = 격자별 표수 × 거리감쇠(≤500=1.0 / 500~800 선형 / >800=0)
- floor_fit: 1층 1.0 / 2층이상 0.7 / 지하 0.5 / 미상 1.0 (config.FLOOR_FIT)
- 경쟁 = vacancy.competitors[업종명] 대비 동네평균(w2=1.0). 헝가리안 배치·차순위 유지.
- 검증: test_engine_v3(19) — 거리감쇠 경계·단조, floor_fit, 손계산, 배치 겹침.

**그 외 (이전 세션, v1 시절 자산):** 포화곡선 %·출처태그·DISCLAIMER·환불 로직·ai_explain 골격은
코드에 존재하나 **v3 포팅 필요**(리포트/캠페인/AI해설이 아직 v1 시그니처).

## 3. 남은 단계 (표)

| 단계 | 내용 | 담당 | 상태 | 검증 기준 |
|---|---|---|---|---|
| **I2** | 리포트 v3: `build_report` 업종→공실 1·2·3위 + 좌표, 포화 곡선 %, 출처 태그, floor 근거 | B | 🔜 다음 | 리스트↔리포트 수치 일치·지도=리포트 score 동일·좌표 |
| **I3** | AI해설 v3: `facts_from_report` v3 형태 + `POST /report`(50냥 차감, A cash 연동) | B | 대기 | 입력 외 숫자 0·금칙 0·키 없으면 null |
| **I4** | 캠페인 환불 v3(region 기반) + 지도 `/map`(placements 상태 vacant/preparing/open) | B·A | 대기 | 기한 후 환불·집계 제외 / 지도 상태 파생 |
| **I5** | 테스트 정리: engine v3 커밋, report/campaign/ai_explain v3 테스트 재작성, demo v3 | B | 대기 | 전 회귀 green |
| **키검증** | 소상공인 `/admin/ingest` 실호출 + AI해설 실호출 (키 확보됨) | 공동 | 가능 | 실응답 파싱·해설 품질 |
| **후순위** | 로그인/세션·소셜, 프런트 연결, 247 코드표, 공실 실면적(건축물대장) | A·프런트 | 대기 | — |

## 4. 컨벤션 (통합 확정)

- **id INTEGER**, **DB meongdang.db**, 키는 **`.env`**(OPENAI_API_KEY·SBIZ_SERVICE_KEY) — **넣음 완료**.
- **격자**: `snap_to_grid` → `"lat,lng"` 3소수 문자열(원좌표 미저장). 엔진이 파싱해 거리 계산.
- **냥**: 10원 = 1냥(A `won_to_nyang`). 투표 1건 = 1,000원 = 100냥. 리포트 생성 = 50냥(예정).
- **경쟁 competitors**: `{업종명: 수}`(A ingest 형태). 엔진은 업종명으로 조회.
- **floor**: 카테고리 `'1층'/'2층이상'/'지하'`(중개사 등록).
- 투자·펀딩 금지 → 선결제·쿠폰. 시드 `is_seed=1`. 결제 모의.

## 5. 실행

```bash
cd claudecode_pkg/code
../.venv/bin/uvicorn main:app --reload      # http://127.0.0.1:8000/docs (.env 자동 로드)
../.venv/bin/python tests/test_v3_data.py   # A 데이터계층 스모크(14)
../.venv/bin/python tests/test_engine_v3.py # 엔진 v3(19)
```

## 6. 열린 결정 (미확정 — 확정처럼 쓰지 말 것)

- 거리감쇠 500/800·floor_fit 0.7/0.5·경쟁 w2=1.0 — 설계 초기값(실데이터 조정)
- K(포화 곡선) 전역/캠페인별 — 리포트 v3(I2)에서 재확정
- 다중투표 1/N vs full — A는 현재 full(각 1건). 1/N은 미구현(향후)
- 공실 실면적 소스(건축물대장), 소상공인 폐업이력(storeListInRadius엔 없음 — 다른 오퍼레이션 조사 필요)
- 로그인·소셜 MVP 포함 여부

## 7. 중요 발견 (A가 실API 검증)

`storeListInRadius` 응답에 **opbizDt/clbizDt(개·폐업일) 필드가 없음** → 폐업(공실 후보) 추출 불가.
공실 추정은 다른 오퍼레이션(`storeListByDate` 등) 별도 조사 필요. (`ingest_api.is_closed` 주석 참조)

## 8. 문서 상태

- **이 문서(11)** = 현재 진실. **10_v2전환_계획** = 전환 로드맵(I0·I1 완료 반영).
- 00~09 = v1/v2 스냅샷(역사적 참고). 08 = v3 명세 원본(산식·리포트·중개사·floor_fit — 유효).
- 09 = A/B 분담(유효). 나머지(01~07)는 배경.
